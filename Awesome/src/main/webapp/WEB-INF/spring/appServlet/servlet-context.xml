<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
    xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:websocket="http://www.springframework.org/schema/websocket"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc          https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context      https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/websocket    http://www.springframework.org/schema/websocket/spring-websocket-4.3.xsd">

<context:property-placeholder location="classpath:application.properties,classpath:aes.properties"/>


    <!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
    
    <!-- Enables the Spring MVC @Controller programming model -->
    <mvc:annotation-driven/>

    <!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
    <mvc:resources mapping="/resources/**" location="/resources/" />

    <!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>
  
   <context:component-scan base-package="com.pcwk.ehr" />
   
   <context:property-placeholder location="classpath:aes.properties"/>
   
   
	<!-- websocket 설정 DispatcherServlet  -->		
	<mvc:annotation-driven />
	
	<websocket:message-broker application-destination-prefix="/send">
	    <!-- 클라에서 SockJS('/realtime-chat') 로 붙는 주소 -->
	    <websocket:stomp-endpoint path="/realtime-chat">
	        <websocket:sockjs/>
	    </websocket:stomp-endpoint>
	
	    <!-- 서버가 convertAndSend 하는 브로커 prefix -->
	    <websocket:simple-broker prefix="/topic"/>
	</websocket:message-broker>
	   
   
   
    
    <!-- 관리자 전용 인터셉터: /admin/** -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <beans:bean class="com.pcwk.ehr.interceptor.AdminOnlyInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
    
</beans:beans>
